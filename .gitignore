.env

# Documentation / specs
docs/

# Python
__pycache__/
*.py[cod]

# Virtual environments
apart_env/
persuasion_env/
.venv/
venv/
env/

# Activation outputs (large binaries — shared out-of-band, not via git).
# The folder itself stays tracked via .gitkeep so runs have somewhere to write.
data/activations/*
!data/activations/.gitkeep
